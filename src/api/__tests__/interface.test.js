describe('APIInterface.post()', () => {
    it('')
})

describe('APIInterface.get()', () => {
    it('')
})

describe('APIInterface.put()', () => {
    it('')
})

describe('APIInterface.delete()', () => {
    it('')
})

describe('APIInterface.getAll()', () => {
    it('')
})